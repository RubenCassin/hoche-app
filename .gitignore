# Secrets — never commit
.env
.env.local
.env.*.local

# Dependencies
node_modules/

# Expo / Metro
.expo/
dist/
web-build/
# Export web généré (re-créé par `expo export -p web --output-dir backend/web` ;
# uploadé sur Railway via .railwayignore, donc inutile de le versionner)
backend/web/

# Native
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Base de données + sauvegardes (données utilisateur — jamais dans git)
backend/db/*.db
backend/db/*.db-journal
backend/db/*.json

# Artefacts générés
scripts/.botEngine.bundle.mjs
.railway-login*.txt

# Misc
npm-debug.*
*.log
.DS_Store

# Ancien clone GitHub vide (pas l'app)
OCHE/
